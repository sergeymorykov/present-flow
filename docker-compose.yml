services:
  present-flow:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: present-flow
    environment:
      PORT: 5000
    ports:
      - "5000:5000"
    restart: unless-stopped

